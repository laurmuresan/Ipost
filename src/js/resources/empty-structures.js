import { immutableList, immutableMap, immutableSet } from 'imm-flux-utils';

export const emptyList = immutableList();
export const emptyMap = immutableMap();
export const emptySet = immutableSet();
export const emptyFunction = () => {};
